[project]
name = "ytmtool"
version = "0.1.0"
dependencies = [
    "ytmusicapi>=1.11.4",
]

[project.scripts]
ytmtool = "ytmtool:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
