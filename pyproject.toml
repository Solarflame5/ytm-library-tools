[project]
name = "ytmtool"
version = "0.1.0"
dependencies = [
    "python-dotenv>=1.2.1",
    "tabulate>=0.9.0",
    "ytmusicapi>=1.11.4",
]

[project.scripts]
ytmtool = "ytmtool.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
